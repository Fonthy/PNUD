 Controllers
    User{
        ->getAllUser()
        ->modifyUser(matricule, mame, lastName, fonction, type)
        ->insertUser(matricule, mame, lastName, fonction, type)
        ->findUser(matricule)
        ->deleteUser(matricule) //Normally we don't delete data
    }
Models
    User{
        (matricule, mame, lastName, fonction, type)
        ->getAllUser()
        ->modifyUser()
        ->insertUser()
        ->findUser(matricule)

    }

Routes
    User{
        /User/getAllUser GET
            input : none
            output : {
                success : {
                    status: 200
                    data : {matricule, mame, lastName, fonction, type}
                },
                error {
                    status: 400
                    data: {}
                    message: "Request error"
                }
             }
    }

 Controllers
    User{
        ->getDataUser()
        ->
    }
Models
    User{
        (matricule, name, lastName, fonction, type)
        ->getAllUser()
        ->modifyUser()
        ->insertUser()
        ->findUser(matricule)

    }

Routes
    User{
        /User/getAllUser GET
            input : none
            output : {
                success : {
                    status: 200
                    data : {matricule, mame, lastName, fonction, type}
                },
                error {
                    status: 400
                    data: {}
                    message: "Request error"
                }
             }
    }
